# Morphological opacity via form→meaning residuals

Per-word **opacity** = how poorly a word's meaning can be predicted from its
written form. Opaque words (monomorphemes like *dog*, lexicalized compounds
like *laptop*) must be memorized; transparent ones (*electrocardiogram*) can
be recovered from their parts.

See the rest of this file after the pipeline is in place.
